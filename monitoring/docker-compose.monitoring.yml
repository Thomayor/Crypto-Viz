version: '3.8'

networks:
  crypto-viz-network:
    external: true
    name: t-dat-901-nce_10_crypto-viz-network

volumes:
  prometheus_data:
  grafana_data:
  loki_data:

services:
  # ======================
  # PROMETHEUS - Metrics Collection
  # ======================
  prometheus:
    image: prom/prometheus:latest
    container_name: crypto-viz-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alerts.yml:/etc/prometheus/alerts.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    networks:
      - crypto-viz-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ======================
  # GRAFANA - Visualization & Dashboards
  # ======================
  grafana:
    image: grafana/grafana:latest
    container_name: crypto-viz-grafana
    restart: unless-stopped
    ports:
      - "3001:3000"  # Port 3001 to avoid conflict with frontend on 3000
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3001
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource,grafana-piechart-panel
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - prometheus
      - loki
    networks:
      - crypto-viz-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ======================
  # LOKI - Log Aggregation
  # ======================
  loki:
    image: grafana/loki:latest
    container_name: crypto-viz-loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    volumes:
      - ./loki/loki-config.yml:/etc/loki/local-config.yaml
      - loki_data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - crypto-viz-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3100/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ======================
  # PROMTAIL - Log Shipper for Loki
  # ======================
  promtail:
    image: grafana/promtail:latest
    container_name: crypto-viz-promtail
    restart: unless-stopped
    volumes:
      - ./loki/promtail-config.yml:/etc/promtail/config.yml
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    networks:
      - crypto-viz-network

  # ======================
  # NODE EXPORTER - System Metrics
  # ======================
  node-exporter:
    image: prom/node-exporter:latest
    container_name: crypto-viz-node-exporter
    restart: unless-stopped
    ports:
      - "9100:9100"
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    networks:
      - crypto-viz-network

  # ======================
  # CADVISOR - Container Metrics
  # ======================
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: crypto-viz-cadvisor
    restart: unless-stopped
    ports:
      - "8086:8080"  # Port 8086 to avoid conflicts
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    privileged: true
    devices:
      - /dev/kmsg
    networks:
      - crypto-viz-network

  # ======================
  # KAFKA EXPORTER - Kafka Metrics
  # ======================
  kafka-exporter:
    image: danielqsj/kafka-exporter:latest
    container_name: crypto-viz-kafka-exporter
    restart: unless-stopped
    ports:
      - "9308:9308"
    command:
      - '--kafka.server=kafka:29092'
    networks:
      - crypto-viz-network
    depends_on:
      - prometheus

  # ======================
  # ALERTMANAGER - Alert Management
  # ======================
  alertmanager:
    image: prom/alertmanager:latest
    container_name: crypto-viz-alertmanager
    restart: unless-stopped
    ports:
      - "9093:9093"
    volumes:
      - ./prometheus/alertmanager.yml:/etc/alertmanager/alertmanager.yml
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    networks:
      - crypto-viz-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9093/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
